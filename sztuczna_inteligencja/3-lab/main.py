from picture import *
import numpy as np

if __name__ == '__main__':
    # S = list((genPermForRow(5, [1, 3])))
    # S.sort()
    # print(S)
    # for x in S:
    #     print(x)

    # p = Picture()
    # p.genPerms()
    # # p.showTab()
    # # print(p.size)
    # # for i in range(2):
    # #     print(p.size[i], p.data[i], [len(x) for x in p.perms[i]])
    # # for i in range(p.size[Row]):
    # #     p.deductRow(checkType(Row), i)
    # # p.show()
    # while not p.isSolved():
    #     p.detuctAll()
    #     # for i in range(2):
    #         # print(p.size[i], p.data[i], [len(x) for x in p.perms[i]])
    #     # p.show()
    # p.saveOtput()
